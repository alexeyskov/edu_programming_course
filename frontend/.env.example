# always — только встроенные демонстрационные данные
# auto   — сначала API, fallback на demo при сетевой ошибке (только development)
# never  — только реальный API
VITE_DEMO_MODE=auto
VITE_API_BASE=/api/v1
VITE_DEV_LOGIN=true
VITE_APP_NAME=Мехмат.Практикум
